struct Node *posNode;
    // posNode=(struct Node*)malloc(sizeof(struct Node));
    // posNode->data=25;
    // int position;
    // printf("Enter the position to insert the node:");
    // scanf("%d",&position);
    // for (int i = 0; i < position-1; i++)
    // {
    //     Head=Head->next;
    // }
    // posNode->next=Head->next;
    // Head->next=posNode;